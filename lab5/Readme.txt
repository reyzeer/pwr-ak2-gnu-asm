cpuid - wzaleznosci od wartosci rejestru rax (nas iteresuje rax = 0), czyszczenie potkow,
rdtscp -m kopiowaie wartosci licznia rax do rbx (32 - bez znaku),
, rdtsc

lscpu -
cat proc/cpuid

gcc -S
-OS - minimalizacja rozmairu kodu binarneg który jest generowany,


gcc -vv

funckja która przyjmuje warotści 0, lub inną wartość

Zaimplementowanie dwoch funkcji w assmelberze uruchamianych z poziomu jezyka C.
Funckje:
f(x) = sqrt(x^2+1)-1
g(x) = x^2/sqrt(x^2+1)

s = stat

Następny raz:
- Przeczytać o rejestrach SIMD,
- Oszacowanie czasu trwania kawałka kodu (liczenie cykli),
- Pokazanie że będzie szybciej robią operacj

gdb:

info registers all - wszystkie rejestry
info float
